<div class="bus-stop-details">
    {% block content %}
        <h4>{{ przystanek.nazwa }}</h4>
        <p>📅 <span class="badge badge-secondary">Data i godzina: </span> {{ przystanek.data }} ( 💻 <span class="badge badge-secondary">IP: </span> {{ przystanek.ip }}) - 🌐 <span class="badge badge-secondary">Przeglądarka: </span> {{ przystanek.browser }}  🚏 <span class="badge badge-secondary">Adres: </span> {{ przystanek.adres }}</p>
        <p>{{ przystanek.opis }}</p>
        {% if images != false %}
            <h5>📷 Fotografie przystanków</h5>
            {% include 'images.html.twig' %}
        {% else %}
            <p class="alert alert-secondary" role="alert">🚫 Brak zdjęć wgranych przez użytkownika</p>
        {% endif %}
        <button data-id="{{ przystanek.id }}" type="button" class="btn btn-dark btn-hide">Ukryj</button>
    {% endblock %}
</div>